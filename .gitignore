# deno vendor/lock (deno.lock 应提交，无需忽略)

# node_modules (deno install 生成)
node_modules

# output (仅忽略仓库根目录的构建产物，vendor/ 下的 dist 需入库)
/out
/dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store
nohup*
release/**